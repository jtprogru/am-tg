apiVersion: v1
kind: Service
metadata:
  name: {{ include "am-tg.fullname" . }}
  labels:
    {{- include "am-tg.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "am-tg.selectorLabels" . | nindent 4 }}
