server {
    listen 80;

    # Any bot API call succeeds; body is close enough to the real thing.
    location / {
        default_type application/json;
        return 200 '{"ok":true,"result":{"message_id":1}}';
    }
}
